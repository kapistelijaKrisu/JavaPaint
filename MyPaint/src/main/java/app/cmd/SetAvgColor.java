
package app.cmd;

import java.awt.Graphics2D;
import java.awt.image.BufferedImage;
import tools.Area;

public class SetAvgColor implements CMD {

    @Override
    public void execute(BufferedImage img, Area area) {
      //  img.getRGB(0, 0, 0, 0, rgbArray, 0, 0)
    }
 
}
